"""
src package marker

This file makes `src` a python package so subpackages can be imported
using package-relative imports (e.g. `from src.mcp import ...` or
relative imports inside package modules).
"""
